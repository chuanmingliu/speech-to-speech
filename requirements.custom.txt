# Lean runtime deps for the Tencent / DeepSeek / MiniMax daemon image.
# Intentionally excludes local STT/TTS stacks (parakeet, qwen3-tts) that
# pull manylinux_2_39-only wheels and are unused by this cascade.
fastapi>=0.115.0
httpx>=0.28.0
nltk==3.9.4
numpy>=1.26.0
openai==2.28.0
pillow>=10.0.0
pydantic>=2.0
rich>=13.0
scipy>=1.10.0
soundfile>=0.13.0
torch>=2.4.0
uvicorn>=0.30.0
websockets>=12.0
tencentcloud-sdk-python-asr>=3.0.0
